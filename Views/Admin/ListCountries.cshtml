@* @model IEnumerable<LandingPage.Models.SwiperModel>

@{
	ViewData["Title"] = "ListCountries";
	Layout = "~/Views/Shared/_AdminLayuot.cshtml";
}

<h1>ListCountries</h1>

<p>
	<a asp-action="Create">Create New</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.CountryName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.PathToPicture)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Tags)
			</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.CountryName)
				</td>
				<td>
					<img src="@item.PathToPicture" width="500" height="500" />
					@Html.DisplayFor(modelItem => item.PathToPicture)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Tags)
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
					<a asp-action="Details" asp-route-id="@item.ID">Details</a> |
					<a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>
 *@